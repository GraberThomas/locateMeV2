export type MarkerType = {
    id:number,
    name: string,
    description: string,
    type: string,
    longitude: number,
    lattitude: number
}

export type CoordinateObject = {
    lng:number,
    lat:number
}

